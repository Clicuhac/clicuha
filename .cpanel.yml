deployment:
  tasks:
    - export DEPLOY_PATH=/home3/clicuhac/public_html
    - rsync -a --no-perms --omit-dir-times --exclude '.git' --exclude '.cpanel.yml' --exclude 'README.md' --exclude '.well-known' 
--exclude 'cgi-bin' --exclude 'logs' ./ "$DEPLOY_PATH"/
  - curl -s "https://clicuha.com/deploy/hook.php?token=${CLICUHA_DEPLOY_TOKEN}"






